# filename: docker-compose.yml
version: '3.9'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: huduu
      POSTGRES_USER: huduu_user
      POSTGRES_PASSWORD: markhoyor
    # порт нээхгүй — зөвхөн internal network дотор
    restart: always

  backend:
    build:
      context: ./backend
    volumes:
      - ./backend/media:/app/media
    ports:
      - "127.0.0.1:8010:8010"
    env_file:
      - ./backend/.env
    depends_on:
      - db
    restart: always

  frontend:
    build:
      context: ./frontend
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      - backend
    environment:
      - INTERNAL_API_URL=http://backend:8010/api
    env_file:
      - ./frontend/.env.local
    restart: always

volumes:
  postgres_data:
